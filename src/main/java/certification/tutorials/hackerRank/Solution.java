package certification.tutorials.hackerRank;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;

public class Solution {

	public static void main(String[] args) throws IOException {

		int sumInt;
		double sumDouble;
		String stringContact = "";
		BufferedReader bufferedReader = new BufferedReader(new InputStreamReader(System.in));

		sumInt = Integer.parseInt(bufferedReader.readLine().trim());

		System.out.println(sumInt += i);
		BufferedReader reader = new BufferedReader(new InputStreamReader(is));
		StringBuilder sb = new StringBuilder();

		while ((stringContact = reader.readLine()) != null) {
			sb.append(s);
			sb.append(stringContact + "\n");

		}
		System.out.println(sb);
		BufferedReader buffered = new BufferedReader(new InputStreamReader(System.in));

		sumDouble = Double.parseDouble(buffered.readLine().trim());
		System.out.println(sumDouble += d);

		buffered.close();
		bufferedReader.close();
		reader.close();
	}

}
